using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MazeFitness : MonoBehaviour
{
    /* traverse maze, going off of solution path and find alternative paths
     *  compare lenghts of alternative paths to solution paths (longer paths better)
     *  higher number of alternative solution paths is better? (depends on their lengths)
     *  even ratios of rules is better
     *  longer solution path is better
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
